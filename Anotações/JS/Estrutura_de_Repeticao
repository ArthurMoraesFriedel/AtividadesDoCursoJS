/*
A estrutura para, não é adequada para situações aonde não sei quantas vezes será repetido


Para (variavel = inicio; ate o fim; [passo salto para + ou -]){


}

*/

for (var i = 2; i <= limite; i++){
    if(numero % i === 0){
        divisores++
    }
}
/*
array.reduce(callback, valorInicial)
callback: Uma função que é chamada para cada elemento do array. 
Ela recebe quatro argumentos: acumulador, elementoAtual, índiceAtual e array.

valorInicial: Um valor opcional que é usado como o valor inicial do acumulador. 
Se não for fornecido, o primeiro elemento do array será usado como o valor inicial do acumulador.

A função de callback é chamada para cada elemento do array, e o valor retornado pela função é utilizado como o acumulador para a próxima chamada. 
No final do processo, o método reduce retorna o valor final do acumulador.
*/